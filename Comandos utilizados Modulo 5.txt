Comandos utilizados practica 1:

sudo dnf install rsync -y
sudo systemctl status rsyncd
ssh-copy-id 
rsync -avz /home/victoramparo/Desktop/carpeta1 victoramparo@192.168.100.146:/home/victoramparo
sudo nano /comando_script.sh
rsync -avz /home/victoramparo/Desktop/carpeta1 victoramparo@192.168.100.146:/home/victoramparo
sudo chmod +x /comando_script.sh
crontab -e

Comandos utilizados practica 2:

sudo dnf -y install pcs pacemaker corosync resource-agents
sudo systemctl enable --now pcsd
sudo passwd hacluster                   
sudo nano /etc/hosts
sudo pcs property set stonith-enabled=false
sudo pcs property set no-quorum-policy=ignore
sudo pcs host auth node1 node2 -u hacluster -p <contraseña>
sudo pcs cluster setup ha-cluster node1 node2
sudo pcs cluster start --all
sudo pcs cluster enable --all
sudo pcs status
sudo pcs resource create FloatingIP ocf:heartbeat:IPaddr2 ip=192.168.100.150 cidr_netmask=24 nic=ens33 op monitor interval=30s
sudo pcs resource start FloatingIP
sudo pcs status resources


